//! Host crate for lesson snippets. `npm run check:rust` writes each standalone
//! program into src/bin and each test file into tests, then builds them here
//! against these pinned dependencies. Both generated directories are git-ignored.
