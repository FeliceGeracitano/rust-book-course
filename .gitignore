# Rust
/target
**/target
Cargo.lock

# Node / client (Plan #2)
node_modules
client/dist
# Course content baked into the client build (regenerated by scripts/prepare-content.mjs).
client/public/course

# Vercel CLI + local secrets (created by `vercel link` / `vercel env pull`).
.vercel
.env*

# OS / editor
.DS_Store
*.swp
.idea
.vscode

# Tooling
.serena/

# Learner's working files — the canonical exercise lives in chapters/*/.exercise.rs;
# src/lib.rs is materialized from it and edited freely, so we don't track it.
chapters/*/src/lib.rs
